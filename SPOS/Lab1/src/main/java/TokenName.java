public enum TokenName {
    COMMENT,
    WHITESPACE,
    IDENTIFIER,
    OPERATOR,
    SEPARATOR, //( ) { } [ ] ; , . ... @ ::
    INT_LITERAL,
    FLOAT_LITERAL,
    CHAR_LITERAL,
    STRING_LITERAL,
    ERROR
}
